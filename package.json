{
  "name": "private-cloud-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler d1 migrations apply PRIVATE_CLOUD_DB --remote --yes && wrangler deploy"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
